# Bootstrap visual evidence

These artifacts were captured from real Chromium rendering on 2026-08-22.

- `logo-concepts.png` — three independent throwaway directions used for the
  selection decision; only Direction A was recreated as production artwork.
- `home-375.png`, `home-768.png`, `home-1024.png`, `home-1440.png` — the
  pre-launch landing page at the four agreed responsive viewports.

Regenerate website screenshots after starting `make serve` and record the
source commit in the related pull request. Do not treat screenshots as the
test oracle; `make check` remains authoritative.
